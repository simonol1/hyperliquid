version: '3.8'

services:
  redis:
    image: redis:7
    container_name: redis
    ports:
      - "6379:6379"

  orchestrator:
    build:
      context: .
      target: runner
    container_name: orchestrator
    env_file: .env
    environment:
      - REDIS_URL=redis://redis:6379
      - HYPERLIQUID_AGENT_PRIVATE_KEY=${HYPERLIQUID_AGENT_PRIVATE_KEY}
      - HYPERLIQUID_WALLET=${HYPERLIQUID_WALLET}
      - HYPERLIQUID_VAULT_ADDRESS=${HYPERLIQUID_VAULT_ADDRESS}
    command: ["node", "dist/orchestrator/index.js"]
    depends_on:
      - redis

  trend_bot:
    build:
      context: .
      target: runner
    container_name: trend_bot
    env_file: .env
    environment:
      - REDIS_URL=redis://redis:6379
      - BOT_TYPE=trend
      - HYPERLIQUID_WALLET=${HYPERLIQUID_WALLET}
      - HYPERLIQUID_VAULT_ADDRESS=${HYPERLIQUID_VAULT_ADDRESS}
    command: ["node", "dist/bots/index.js"]
    depends_on:
      - redis

  breakout_bot:
    build:
      context: .
      target: runner
    container_name: breakout_bot
    env_file: .env
    environment:
      - REDIS_URL=redis://redis:6379
      - BOT_TYPE=breakout
      - HYPERLIQUID_WALLET=${HYPERLIQUID_WALLET}
      - HYPERLIQUID_VAULT_ADDRESS=${HYPERLIQUID_VAULT_ADDRESS}
    command: ["node", "dist/bots/index.js"]
    depends_on:
      - redis

  reversion_bot:
    build:
      context: .
      target: runner
    container_name: reversion_bot
    env_file: .env
    environment:
      - REDIS_URL=redis://redis:6379
      - BOT_TYPE=reversion
      - HYPERLIQUID_WALLET=${HYPERLIQUID_WALLET}
      - HYPERLIQUID_VAULT_ADDRESS=${HYPERLIQUID_VAULT_ADDRESS}
    command: ["node", "dist/bots/index.js"]
    depends_on:
      - redis
